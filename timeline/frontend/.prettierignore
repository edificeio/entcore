# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
/.pnpm-store

# Auto-generated by msw's postinstall (`msw init`) on every `pnpm install`.
# Never reformat it: any diff here just makes it drift from MSW's own output.
/public/mockServiceWorker.js