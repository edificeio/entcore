import { App } from '~/components/App';

/** Check old format URL and redirect if needed */
export const loader = async () => {
  return null;
};

export const Root = () => {
  return <App />;
};

export default Root;
