import { VHTMLDocument } from './index.browser.cjs';
export { VDocType, VDocument, VDocumentFragment, VElement, VElementStyle, VNode, VNodeQuery, VTextNode, createDocument, createHTMLDocument, document, escapeHTML, h, hArgumentParser, hFactory, handleHTML, hasOwn, isVDocument, isVElement, isVTextElement, parseHTML, removeBodyContainer, safeHTML, serializeMarkdown, serializePlaintext, serializeSafeHTML, tidyDOM, unescapeHTML, vdom, xml } from './index.browser.cjs';
export { C as CDATA, h as html } from './jsx-runtime-BU61zbMI.cjs';

/** Manipulate HTMl file directly on disk. Only writes back if there were significant changes. */
declare function handleHTMLFile(filePath: string, handler: (document: VHTMLDocument) => void, outPath?: string): string;

export { VHTMLDocument, handleHTMLFile };
