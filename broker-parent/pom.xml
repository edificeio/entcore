<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.entcore</groupId>
        <artifactId>ent-core</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>broker-parent</artifactId>
    <packaging>pom</packaging>
    <properties>
        <nats.version>2.1.0</nats.version>
        <jnats.version>2.20.5</jnats.version> <!-- this version should match the one corresponding to ${nats.version} -->
    </properties>

    <modules>
        <module>broker-api</module>
        <module>broker</module>
        <module>broker-nats</module>
    </modules>
</project>